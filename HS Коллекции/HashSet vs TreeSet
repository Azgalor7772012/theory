->HashSet - коллекция, основання на хэштаблице. При добавлении эл-та в коллекция, смотрится его хэшкод,
по хэшкоду(хэшкод % длину массива) выбирается ячейка, куда пойдет элемент.
При добавлении такого же элемента проверяется есть ли уже такой элемент в этой ячейке, если есть он не
добавится.
Если 2 разных элемента имеют одинаковый хэшкод, то элемент добавляется в поле элемента в массиве, то есть
образуется подобие LinkedList. Это называется коллизией, при коллизии сложность добавления и удаления
возрастает до O(N)

Таким образом в хэшсете:
1)Не может быть одинаковых элементов
2)Добавление и удаление эл-тов - O(1) если нет колизии
3)Нельзя добавлять и удалять эл-ты по индексу т.к там все основано на хэшкоде

->TreeSet - такая же коллекция, как ХэшСет, но все элементы отсортированы.
Чтобы добавить эл-т, он должен имплементировать интерфейс Comparable и должен быть переопределен метод
compareTo. Либо любой объект, но в конструкторе TreeSet должен быть передан Comporator
Сложность всех операций в TreeSet - O(log(N))
