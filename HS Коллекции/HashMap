-HashMap - коллекция, основанная на хэштаблице, но при этом в ячейки кладутся объекты ключ и значение.
Место в таблице рассчитывается по хэшкоду ключа.

->HashMap наследуется от интерфейса Map, который не связан с интерфейсом Collection.
Он не наследуется ни от коллекшн, ни от итерабл(раз от коллекшн не наследуется).

->2 одинаковых ключа не может быть в коллекции

Работает так:
C помощью метода put(Key, Value) подается объекты, проверяется по хэшкоду ключа в какую ячейку класть,
если в ячейке уже есть элемент, то с помощью equals проверяется равны ли ключи, если они равны, то значение
перезаписывается, если не равны, то создается связанный список(как LinkedList), в поле объекта
находящегося в массиве кладется наша пара ключ-значение, если поле не равно нул, то снова проверяем и так
по кругу, пока не положим или не перезапишем занчение.

Также по умолчанию если коллизия достигла 8 элементов, то вместо LinkedList структура становится как у
TreeSet и вместо сложности O(N), сложность становится O(log(N))