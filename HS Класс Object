Класс Object является прародителем всех остальных классов. Все классы, включая пользовательские
являются классами-наследниками класса Object, поэтому при создание экземпляра любого класса можно ссылаться
на класс Object.
Класс Object предоставляет некоторые общие методы для всех подклассов. Он имеет девять методов
 экземпляра (исключая перегруженные методы), которые можно разделить на четыре группы:
1)Синхронизация потоков : wait, notify, notifyAll;
2)Идентификатор объекта : hashCode, equals;
3)Управление объектом : finalize, clone, getClass;
4)Удобочитаемое представление : toString;

Что делают:
1)Первая группа методов ( wait, notify, notifyAll) предназначена для работы в многопоточных приложениях.
2)hashCode возвращает значение хэш-кода для объекта.
3)equals указывает, является ли какой-либо другой объект «равным» этому конкретному.
4)finalize вызывается сборщиком мусора (GC) для объекта, когда GC хочет его очистить.
(Примечание: этот метод устарел, начиная с JDK 9).
5)clone создает и возвращает копию объекта.
6)getClass возвращает экземпляр Class, который содержит информацию о классе среды выполнения.
7)toString возвращает строковое представление объекта.

->Метод instanceOf проверяет является ли объект экземпляром или экземпляром подкласса другого объекта:
if (anObject istanceOf String) {
    return "String";
}
Если проверяем объект со значением null, то выдаст всегда false;

->Есть правило - если переопределяем equals, то нужно переопределить hashcode и наоборот.