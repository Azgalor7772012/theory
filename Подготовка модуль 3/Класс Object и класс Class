-->Класс Object - главный класс в Java, от него наследуются все остальные классы.
1.Это сделано для того, чтобы дочерний класс получил весь функционал родительского класса, то есть
мы делаем так, чтобы какие-то методы были во всех классах + 2.класс наследник может использоваться
в любом месте, где требуется объект базового класса.

->Методы класса Object:
Популярные:
1)hashCode() - отображение объекта в виде числа
2)equals()
3)toString()
Синхронизация потоков - служат для синхронизации потоков и работают в контексте synchronized блоков:
4)notify() - пробуждает один поток
5)notifyAll() - пробуждает все потоки, которые ждут на мониторе
6)wait()  - останавливает поток
Отдельно:
7)getClass()
8)clone() - делает глубокую копию объекта. Не глубокое копирование копирует только ссылки, глубокое - и сами эл-ты.
9)finalize() - устаревший.

